pages:
  stage: deploy
  script:
    - mkdir -p .public
    - cp -r * .public
  artifacts:
    paths:
      - public
  only:
    - main

pages-beta:
  extends: pages
  stage: deploy
  script:
    - mkdir -p .public/beta
    - ls -A
    - ls -A .public/
    - cp -r * .public/beta
  artifacts:
    paths:
      - public/beta
  only:
    - dev

pages-final:
  extends: pages
  stage: deploy
  script:
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main
    - dev