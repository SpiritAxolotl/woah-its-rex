pages:
  stage: deploy
  script:
    - mkdir -p .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main

pages-beta:
  extends: pages
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public/beta
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - dev